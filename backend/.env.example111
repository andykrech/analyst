# === Email (Mailpit для локальной разработки) ===
# Провайдер: smtp | console (переключение только этой переменной)
EMAIL_PROVIDER=smtp
EMAIL_FROM=no-reply@local
# SMTP (Mailpit в docker: host=mailpit, port=1025; без docker: host=localhost)
SMTP_HOST=mailpit
SMTP_PORT=1025
# Фронт для ссылок в письмах
FRONTEND_BASE_URL=http://localhost:5173

# Опционально: SMTP_USERNAME, SMTP_PASSWORD, SMTP_USE_TLS, SMTP_STARTTLS, EMAIL_FROM_NAME

# === Yandex Search API v2 (gRPC) ===
YANDEX_API_KEY=changeme
YANDEX_FOLDER_ID=changeme
YANDEX_SEARCH_ENDPOINT=searchapi.api.cloud.yandex.net:443
YANDEX_OPERATION_ENDPOINT=operation.api.cloud.yandex.net:443
YANDEX_SEARCH_REGION=225
YANDEX_SEARCH_TIMEOUT_SECONDS=10
YANDEX_OPERATION_POLL_ATTEMPTS=10
YANDEX_OPERATION_POLL_INTERVAL_SECONDS=0.5
